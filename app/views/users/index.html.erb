<% provide(:title, 'Guest List') %>
<h1>Who's Attending?</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= user.name %>
    </li>
  <% end %>
</ul>

<div class="scroll">
  <%= will_paginate %>
</div>

<%= render 'layouts/footer' %>
