<% provide(:title, 'Guest List') %>
<h1>Guest List</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= user.name %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>

<%= render 'layouts/footer' %>
