<% provide(:title, 'Guest List') %>
<h1>Who's Attending?</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= user.name %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>

<%= render 'layouts/footer' %>
